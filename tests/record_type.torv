write(f: i32, buf: Ptr(u8), len: usize): i32 @extern("write")

type Foo {
    data: str
    clip: usize
}

msg: Foo = {.data="Hello from record\n...", .clip=3}

main: i32 =
    write(1, @str_ptr(msg.data, 0), @str_len(msg.data) - msg.clip)
