write(f: i32, buf: Ptr(u8), len: usize): i32 @extern("write")

msg := "got 10\n"

rec(i: u8) :=
    if i >= 10 then
        msg
    else
        rec(i + 1)

main: i32 =
    res := rec(0)
    _ := write(1, @str_ptr(res, 0), @str_len(res))
    0
